<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/coffee_bg"
    android:fillViewport="true"
    android:fitsSystemWindows="true"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="50dp"
        android:layout_marginTop="0dp"
        android:gravity="center_horizontal">

        <!-- Logo -->
        <ImageView
            android:id="@+id/logoRegister"
            android:layout_width="110dp"
            android:layout_height="110dp"
            android:src="@drawable/logo_graodavilla"
            android:contentDescription="Logo do app"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/textWelcome"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Crie sua conta no Grão da Villa"
            android:textColor="@color/text_secondary"
            android:textSize="20sp"
            android:fontFamily="@font/roboto_regular"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <TextView
            android:id="@+id/textSubtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Preencha os campos para se cadastrar"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:fontFamily="@font/roboto_regular"
            android:textStyle="normal"
            android:layout_marginBottom="16dp" />

        <!-- Seletor Login/Cadastro -->
        <LinearLayout
            android:id="@+id/switchAuthMode"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:orientation="horizontal"
            android:background="@drawable/bg_switch_border"
            android:gravity="center"
            android:layout_marginBottom="16dp">

            <!-- Botão Login -->
            <Button
                android:id="@+id/buttonTabLogin"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="Login"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/white"
                android:background="@android:color/transparent"
                android:fontFamily="@font/roboto_bold"/>

            <!-- Botão Cadastro -->
            <Button
                android:id="@+id/buttonTabCadastro"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:text="Cadastro"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/text_secondary"
                android:background="@drawable/bg_switch_selected_right"
                android:fontFamily="@font/roboto_bold"/>
        </LinearLayout>

        <LinearLayout
            android:id="@+id/buttonGoogleRegister"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:background="@drawable/bg_edittext_round"
            android:backgroundTint="@color/divider_gray"
            android:paddingHorizontal="16dp"
            android:layout_marginBottom="16dp"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_google"
                android:contentDescription="Ícone Google"
                android:layout_marginEnd="8dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Entrar com Google"
                android:textColor="@color/text_primary"
                android:textStyle="bold"
                android:textSize="16sp"
                android:fontFamily="@font/roboto_bold" />
        </LinearLayout>


        <!-- Nome -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:orientation="horizontal"
            android:background="@drawable/bg_edittext_round"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_person"
                android:contentDescription="Ícone de nome"
                app:tint="@color/text_secondary" />

            <EditText
                android:id="@+id/editRegisterName"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:hint="Nome completo"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary"
                android:background="@android:color/transparent"
                android:textSize="16sp"
                android:inputType="textPersonName"
                android:paddingStart="8dp"
                android:fontFamily="@font/roboto_regular" />
        </LinearLayout>

        <!-- E-mail -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:orientation="horizontal"
            android:background="@drawable/bg_edittext_round"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_email"
                android:contentDescription="Ícone de e-mail"
                app:tint="@color/text_secondary" />

            <EditText
                android:id="@+id/editRegisterEmail"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:hint="E-mail"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary"
                android:background="@android:color/transparent"
                android:textSize="16sp"
                android:inputType="textEmailAddress"
                android:paddingStart="8dp"
                android:fontFamily="@font/roboto_regular" />
        </LinearLayout>

        <!-- Senha -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:orientation="horizontal"
            android:background="@drawable/bg_edittext_round"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_password"
                android:contentDescription="Ícone de senha"
                app:tint="@color/text_secondary" />

            <EditText
                android:id="@+id/editRegisterPassword"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:hint="Senha"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary"
                android:background="@android:color/transparent"
                android:textSize="16sp"
                android:inputType="textPassword"
                android:paddingStart="8dp"
                android:fontFamily="@font/roboto_regular" />
        </LinearLayout>

        <!-- Confirmar Senha -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:orientation="horizontal"
            android:background="@drawable/bg_edittext_round"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:gravity="center_vertical"
            android:layout_marginBottom="16dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/ic_password"
                android:contentDescription="Ícone de confirmar senha"
                app:tint="@color/text_secondary" />

            <EditText
                android:id="@+id/editRegisterConfirmPassword"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:hint="Confirmar senha"
                android:textColor="@color/text_primary"
                android:textColorHint="@color/text_secondary"
                android:background="@android:color/transparent"
                android:textSize="16sp"
                android:inputType="textPassword"
                android:paddingStart="8dp"
                android:fontFamily="@font/roboto_regular" />
        </LinearLayout>

        <!-- Botão Cadastrar -->
        <Button
            android:id="@+id/buttonRegister"
            android:layout_width="match_parent"
            android:layout_height="45dp"
            android:text="Cadastrar"
            android:textColor="@color/text_primary"
            android:textStyle="bold"
            android:textSize="16sp"
            android:backgroundTint="@color/accent_beige"
            android:layout_marginBottom="16dp"
            android:fontFamily="@font/roboto_bold" />

    </LinearLayout>
</ScrollView>
